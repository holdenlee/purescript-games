module Snake where

import Prelude
import Control.Monad.Eff (Eff)
import Control.Monad.Eff.Console (CONSOLE, log)

main :: Eff _ Unit
main = do
  x <- randInt 1 10
  log (show x)
  log (show x)
  log (show x)
  pure unit
